# control 控制模块说明

本目录负责视觉伺服中的控制量计算。

## 文件说明

- `visual_servo_p.py`：根据目标像素位置、目标距离和 yaw 计算 P 控制误差与 RC 命令。
- `rc_mapper.py`：把控制增量映射到 RC PWM 数值，并做限幅。
- `filters.py`：简单滑动平均滤波。

## 安全规则

- 当前默认先运行 dry-run，只记录控制量，不直接发送给 ROV。
- 水下测试前应先确认 marker 检测稳定、距离估计合理、RC 限幅足够小。
- 丢失 marker 时应回中位或停止输出，不应继续使用旧控制量。
